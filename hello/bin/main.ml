let () = print_endline "Hello, my friend!"

(* printf function from Printf module *)
let () = Printf.printf "%s\n" "Hello, World!" 

(* Using variables from other modules *)
let () = Printf.printf "%s\n" Hello.En.v
